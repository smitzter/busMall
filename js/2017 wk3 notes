function generate to var with Math to pint to the array.
image productNameParent

//check for duplicates
check is the imageName is present in the duplicates array
//how to check if something is present in an Array.
//array.prototype.includes()
//Array.prototype.indexof()
while ( x || y  ==...) {
image1 = generateRandomProduct();
...
...
}

duplicate.push(imageX);
...
...

function  MakeImages(name, filepath, timeShown, timeSelected) {
this.name = name,
this
this
this
}

var arrayOfObjects = [];
for (var i = 0; i < imageNames.length; i++) {
arrayof Objects.push(new MakeImages(imageNames[i]));
}
//need to clear the page.
//clear = the current three images need to go away
//removeChild
//reset src
//hide image - lots of hidden elements clog up page.
//function ProductName(name, file path) {
this.name = name,
this.filepath = filepath,
this.click
}

//daniel

var imageDict = {
  'bag': new Product({'idHTML',: 'bag', 'imageName', 'bag.jpg', 'filePath': 'assets/'
  })
  ...
  ...
}
image(dogDuck);

//do/while loop
//counter
//boolean

//demo
